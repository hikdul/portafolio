import { JSX } from "react"

interface leftBarProps
{
    fullScreen: boolean
} 


interface dataIconLBProps {
    name: string
    desc: string
    icon: string 
}

export type {
    leftBarProps,
    dataIconLBProps
}